# DL-04 RAG System Development I
# ทดสอบแล้วบน macOS (Apple Silicon) + Python 3.11
# ติดตั้ง:  pip install -r requirements.txt

sentence-transformers>=5.0      # โมเดล embedding + cross-encoder reranker
faiss-cpu>=1.9                  # ฐานข้อมูลเวกเตอร์ (dense retrieval)
rank_bm25>=0.2.2                # ดัชนี BM25 (keyword retrieval)
pythainlp>=5.0                  # ตัดคำไทยให้ BM25 — ขาดตัวนี้คะแนน BM25 จะตกอย่างมาก
numpy>=1.26
openai>=1.40                    # ใช้เป็น client กลาง เรียกได้ทั้ง ollama / openai / gemini
python-dotenv>=1.0              # อ่านคีย์ LLM จากไฟล์ .env ที่ไม่ถูก commit

# เว็บถาม-ตอบ (api.py + web/) — ไม่จำเป็นถ้าใช้แค่ main.py บน terminal
fastapi>=0.115                  # REST API หุ้ม RAGPipeline ตัวเดิม
uvicorn[standard]>=0.30         # เซิร์ฟเวอร์ที่รัน api.py
