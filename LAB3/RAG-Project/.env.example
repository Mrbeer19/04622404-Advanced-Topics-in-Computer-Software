# คัดลอกไฟล์นี้เป็น .env แล้วใส่คีย์ของตัวเอง (ไฟล์ .env ไม่ถูก commit ขึ้น git)
# gemini  -> https://aistudio.google.com/apikey
GOOGLE_API_KEY=
# openai  -> ใช้เมื่อตั้ง LLM_PROVIDER = "openai"
OPENAI_API_KEY=
